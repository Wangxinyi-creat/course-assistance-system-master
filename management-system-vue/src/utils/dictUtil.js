

/**
 * 获取字典名称
 * @param dictType  字典类型
 * @param dictValue  字典值
 */
export function getDictLabel(dictType, dictValue) {
    let value = ''
    // const res = await getByDictType(dictType)
    // const dataList = res.data
    // dataList.forEach(item => {
    //     if (dictValue.toString() === item.dictValue) {
    //         value = item.dictLabel
    //     }
    // })
    // return '男'
}
